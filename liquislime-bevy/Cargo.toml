[package]
name = "liquislime-bevy"
version = "0.1.0"
edition = "2021"

[features]
bevy-host = []

[dependencies]
bevy = "0.9.1"
derive_more = "0.99.17"
fp-bindgen-support = { path = "../fp-bindgen/fp-bindgen-support", features = ["host", "async"] }
serde = { version = "1.0.152", features = ["derive"] }

[target.'cfg(target_arch = "wasm32")'.dependencies]
wasmer = { version = "2.3", features = ["js-default", "wasmer-compiler-singlepass", "wasmer-engine-universal"], default-features = false}
js-sys = "0.3"
wasm-bindgen = "0.2"

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
wasmer = { version = "2.3", features = ["singlepass"]}

# Enable a small amount of optimization in debug mode
[profile.dev]
opt-level = 0

# Enable high optimizations for dependencies (incl. Bevy), but not for our code:
[profile.dev.package."*"]
opt-level = 0 # TODO: change back to 3 once the build process is stable
